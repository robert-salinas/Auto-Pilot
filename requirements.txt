# ---------------------------------------------------------------------------
# Dependencias de AutoPilot RPA
# Instalación automática:  ejecutar  instalar.bat
# Instalación manual    :  pip install -r requirements.txt
# ---------------------------------------------------------------------------

PySide6>=6.6.0            # Interfaz gráfica moderna (Qt 6)
pyautogui>=0.9.54         # Control visual del ratón y el teclado
pywinauto>=0.6.8          # Control nativo de ventanas de Windows
Pillow>=10.0.0            # Requerido por PyAutoGUI para capturas de pantalla
opencv-python>=4.8.0      # Habilita el parámetro 'confidence' en la búsqueda de imágenes
python-dotenv>=1.0.0      # Lectura y escritura del archivo .env
cryptography>=42.0.0      # Cifrado AES (Fernet) de las credenciales
keyboard>=0.13.5          # Atajo global de parada de emergencia (CTRL+ALT+Q)
pynput>=1.7.6             # Captura simultánea en vivo de clics, ratón y teclado

